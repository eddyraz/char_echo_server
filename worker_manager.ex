defmodule WorkerManager do

  @moduledoc """
  Manejador de workers,
  recibe las peticiones de terminar los workers,
  y emite creacion(spawning de nuevos workers)
  todo se registra en una pagina por el puerto 5066  
  """


  
  end
